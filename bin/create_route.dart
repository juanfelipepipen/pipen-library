// import 'package:interact/interact.dart';
//
// void main(List<String> args) {
//   String routeName = Input(
//     prompt: 'Name for route, ex. config.email.update',
//     validator: (String x) => x.isNotEmpty ? true : throw ValidationError('Name for route required'),
//   ).interact();
//
//   String dir = Input(
//     prompt: 'Directory for save, ex. config/email/update',
//     validator: (String x) => x.isNotEmpty ? true : throw ValidationError('Name for route required'),
//   ).interact();
// }
// //
